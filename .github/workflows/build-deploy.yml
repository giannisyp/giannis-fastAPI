name: Build and Deploy Code

on: [push,pull_request]

jobs:
  job1: windows-latest
  steps:
    -name: pulling git repo
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: say hi to giannis
        run: echo "hello giannis"
